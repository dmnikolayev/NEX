export class LayoutManager{constructor(scene,bg){this.scene=scene;this.bg=bg;this.layouts={}}async load(){for(const n of["desktop","tablet","mobile"]){this.layouts[n]=await(await fetch(`layout/${n}.json`,{cache:"no-store"})).json()}this.apply();addEventListener("resize",()=>this.apply(),{passive:true})}profile(){return innerWidth<=700?"mobile":innerWidth<=1100?"tablet":"desktop"}apply(){const l=this.layouts[this.profile()];this.bg.style.objectPosition=l.camera.objectPosition;for(const[id,c]of Object.entries(l.nodes)){const e=this.scene.querySelector(`[data-node="${id}"]`);if(!e)continue;e.style.setProperty("--x",c.x);e.style.setProperty("--y",c.y);e.style.setProperty("--tx",c.tx??"-50%");e.style.setProperty("--ty",c.ty??"-50%");e.style.setProperty("--scale",c.scale??1)}this.scene.dispatchEvent(new Event("nex:layout"))}}